name: Production backend release (verified)

# Admin and website releases use their own scoped workflows.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verified-release:
    uses: ./.github/workflows/backend-ci.yml
    secrets: inherit
