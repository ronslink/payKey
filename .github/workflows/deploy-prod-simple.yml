name: Production fallback (with required tests)

# Kept as an entrypoint; there is no untested/destructive deployment path.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verified-release:
    uses: ./.github/workflows/backend-ci.yml
    secrets: inherit
