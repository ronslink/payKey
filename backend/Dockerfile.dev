FROM node:20-alpine

WORKDIR /app

# Install Docker CLI for container logs feature
RUN apk add --no-cache docker-cli

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code ONLY (bypass bind mount issues)
COPY . .

# Run dev server
CMD ["npm", "run", "start:dev"]
